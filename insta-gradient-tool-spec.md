# 인스타 그라데이션 텍스트 도구 — 프로젝트 스펙 (컨셉 락 v1)

## 목적
개인 인스타그램용. 사진 하단에 검정 그라데이션 오버레이를 얹고 그 위에 흰색 볼드 텍스트를 넣은 이미지를 만드는 모바일 웹앱. (참고 스타일: 사진 하단 1/3에 검정→투명 그라데이션 + 흰색 굵은 산세리프 텍스트, 하단 왼쪽 정렬)

## 확정 스펙
1. **형태**: 단일 HTML 파일 웹앱 (HTML/CSS/JS 모두 한 파일). 서버 없음, 회원가입 없음. 모든 이미지 처리는 브라우저 안에서만 수행 (사진이 외부로 전송되지 않음).
2. **사용 환경**: 아이폰 Safari 중심의 모바일 브라우저. 터치 조작 최적화. "홈 화면에 추가"로 앱처럼 사용할 예정.
3. **비율**: 1:1 과 4:5 중 선택 가능 (토글 버튼).
4. **출력**: 저장 시 고해상도 캔버스에서 원본 사진으로 다시 렌더링하여 PNG 다운로드.
   - 1:1 → 1080x1080
   - 4:5 → 1080x1350
   - 미리보기 캡처 방식 금지. 반드시 원본 이미지 데이터로 최종 캔버스에 재렌더링할 것.
5. **사진 업로드**: 파일 선택 버튼으로 갤러리에서 선택 (`<input type="file" accept="image/*">`).
6. **크롭**: 사진을 손가락 드래그로 이동, 핀치(또는 슬라이더)로 확대/축소하여 프레임 안에서 원하는 부분을 배치할 수 있게. (인물 사진에서 얼굴 잘림 방지 목적.) 저장 시 이 배치 상태 그대로 고해상도 출력에 반영.
7. **그라데이션**:
   - 색: 검정 고정 (검정→투명, 아래에서 위로)
   - 조절 슬라이더 2개: ① 높이 (하단에서 얼마나 올라올지, 예: 이미지 높이의 20~70%) ② 진하기 (최대 불투명도, 예: 0~90%)
8. **텍스트**:
   - 색: 흰색 고정
   - 위치: 하단 왼쪽 고정 (좌우 여백 약 7%, 하단 여백 약 7% 정도의 세이프 마진)
   - 줄바꿈: 사용자가 입력칸에서 직접 엔터로 줄 나눔 (자동 줄바꿈 아님, textarea 사용)
   - 크기: 슬라이더로 조절
   - 폰트: **나눔스퀘어 네오 (NanumSquare Neo)** 웹폰트로 로드. 굵기 선택 가능 (Bold / ExtraBold / Heavy 정도)
   - 가독성을 위해 약간의 line-height 조절 (1.2~1.3)
9. **UI 구성 (위→아래)**: 미리보기 영역 → 비율 토글 → 사진 선택 버튼 → 텍스트 입력칸 → 슬라이더들(그라데이션 높이/진하기, 텍스트 크기, 폰트 굵기) → 저장 버튼. 한국어 UI.
10. **미리보기**: 모든 조절이 실시간 반영.

## 기술 노트
- 나눔스퀘어 네오 웹폰트: CDN(jsdelivr의 nanumsquare-neo 패키지 등)에서 @font-face 로드. 캔버스 렌더링 전에 `document.fonts.ready` 로 폰트 로딩 완료를 기다린 후 그릴 것 (폰트 미적용 출력 방지).
- iOS Safari에서 canvas.toBlob → 다운로드 링크 방식이 사진 앱 저장과 자연스럽게 연동되는지 확인. 필요시 "이미지를 길게 눌러 저장" 안내 fallback.
- iOS에서 큰 사진(HEIC/고해상도) 처리 시 캔버스 메모리 한계 주의.
- PWA 최소 요건(뷰포트 메타, 아이콘/manifest 인라인 또는 생략 가능) — 홈 화면 추가 시 보기 좋게.

## 배포 (GitHub Pages)
- 사용자는 GitHub 계정 보유. 새 public 리포지토리 생성 → index.html 푸시 → Settings > Pages에서 main 브랜치 배포.
- 클로드 코드가 gh CLI 또는 git으로 리포 생성/푸시/Pages 설정까지 자동화해줄 것.
- 최종 산출물: `https://<username>.github.io/<repo-name>/` 주소.

## 완료 기준
1. 폰 Safari에서 주소 접속 → 사진 선택 → 크롭 조정 → 그라데이션/텍스트 조절 → PNG 저장까지 전 과정 작동
2. 저장된 PNG가 1080px 해상도이고 나눔스퀘어 네오 폰트가 적용되어 있음
3. 홈 화면에 추가하면 아이콘으로 실행 가능
